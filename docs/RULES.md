# Rules

Files and classes should expose as few public methods as possible, and have functionality delegated to private internals where possible. Public methods should be listed first in a file or class, then proceeded with the private functions. The functions should be in call order.
